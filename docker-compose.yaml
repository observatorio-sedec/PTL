services:
  postgres:
    image: postgres:18.1
    container_name: postgres_ptl
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - ptl_scraper_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  etl_ptl:
    build: .
    image: etl_ptl:latest
    container_name: scraper_ptl
    command: ["python", "principal.py"]
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ptl_scraper_net

volumes:
  pgdata:

networks:
  ptl_scraper_net:
    driver: bridge

